from pymongo import MongoClient

# Por padrão o host é localhost e porta 27017
# Estes valores podem ser modificados passando uma URL
# client = MongoClient("mongodb://localhost:27017/")
client = MongoClient()
