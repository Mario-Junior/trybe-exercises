// 5 - Escrever função swap , que dado array de 3 elementos, retorna novo array com o primeiro e terceiro elementos trocados.
// Detalhe: gastando 1 linha só:
// Dica: use array destructuring
const myList = [1, 2, 3];

// escreva swap abaixo
const swap = ([i1, i2, i3]) => [i3, i2, i1];
console.log(swap(myList));
